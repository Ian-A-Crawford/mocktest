version: 0.2

phases:
  build:
    commands:
      - echo "Processing mock test results"

reports:
  junit_reports:
    files:
      - "mock.xml"
      - "mock2.xml"
      - "mock3.xml"
    file-format: "JUNITXML"
